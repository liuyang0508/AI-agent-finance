# core framework
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0

# vector database
chromadb>=0.5.0

# PDF parsing
marker-pdf>=0.2.17

# intent recognition handled by GLM few-shot prompt (no extra dependency)

# evaluation
ragas>=0.2.0

# data processing
pydantic>=2.0.0
python-dotenv>=1.0.0
pandas>=2.0.0

# HTTP client (used by MCP tools to call financial APIs)
httpx>=0.27.0

# Optional: MCP server (install manually if needed)
# mcp>=1.0.0
# langchain-mcp-adapters>=0.1.0

# reranking (cross-encoder)
sentence-transformers>=3.0.0

# utilities
tiktoken>=0.7.0
tenacity>=8.0.0
